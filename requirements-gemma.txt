# Optional dependencies for model-backed Gemma inference
# Keep this separate from base requirements to preserve lightweight offline setup.
transformers>=4.52.0
accelerate>=0.34.0
safetensors>=0.4.5
sentencepiece>=0.2.0
torch>=2.4.0
torchvision
