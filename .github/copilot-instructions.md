

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.10.10 -->
<!-- Updated: 2026-05-18T20:47:31.757Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
scripts/demo_ui.py ← __future__, PIL, reportlab, voicemed, gradio
src/voicemed/config.py ← __future__
src/voicemed/engine/model.py ← __future__, voicemed
src/voicemed/engine/tool_executor.py ← __future__, voicemed
src/voicemed/engine/tools.py ← __future__, voicemed
src/voicemed/output/schemas.py ← __future__
scripts/demo_cli.py ← __future__, voicemed
scripts/evaluate_offline.py ← __future__, voicemed, ast
scripts/run_smoke_checks.py ← __future__
```

## changes (last 10 commits — 13 hours ago)
```
scripts/demo_ui.py                            +_translate_via_ollama  +_severity_badge  +_build_referral_pdf  +_parse_voice_fields
```

## .github

### .github/copilot-instructions.md
```
h2 Auto-generated signatures
h2 SigMap commands
h1 Code signatures
h2 deps
h2 src
h3 src/voicemed/config.py
h3 src/voicemed/engine/model.py
h3 src/voicemed/engine/tool_executor.py
h3 src/voicemed/engine/tools.py
h3 src/voicemed/output/schemas.py
code-fence plain
```

### .github/workflows/ci.yml
```
keys: [name, on, jobs]
job: smoke-checks
```

## scripts

### scripts/demo_ui.py
```
def backend_status_html() → str
def transcribe_audio_to_text(audio_path: str | None, current_text: str | None) → tuple[str, str, int | None,...  # Transcribe audio and extract name/age/weight into separate f
def assess_case(text: str, patient_name: str | None, age: float | None, weight: float | None, image_path: str | None, audio_path: str | None, language: str) → tuple[str, dict, str, str |...
def build_ui() → gr.Blocks
def parse_args() → argparse.Namespace
def main() → None
```

### scripts/demo_cli.py
```
def parse_args() → argparse.Namespace
def main() → None
```

### scripts/evaluate_offline.py
```
@dataclass CaseResult(case_id, expected, predicted, description, exact_match, distance)
def load_test_cases(path: Path) → list[dict]
def evaluate(cases: list[dict]) → dict
def main() → None
```

### scripts/run_smoke_checks.py
```
def run_step(name: str, cmd: list[str], env: dict[str, str] | None) → None
def main() → None
```

## src

### src/voicemed/config.py
```
@dataclass Settings(model_id, enable_model_inference)
```

### src/voicemed/engine/model.py
```
class Gemma4TriageEngine
  def __init__() → None
  def get_debug_snapshot() → dict[str, Any]
  def load() → None
```

### src/voicemed/engine/tool_executor.py
```
class ToolExecutor
  def execute(name: str, arguments: dict[str, Any]) → dict[str, Any]
```

### src/voicemed/engine/tools.py
```
def assess_triage_severity(primary_symptom: str, duration_hours: int, consciousness_level: str, breathing_difficulty: bool, bleeding_severity: str) → dict[str, Any]
def lookup_treatment_protocol(condition: str, setting: str) → dict[str, Any]
def check_medication_availability(medication_name: str, required_dose_mg: float, route: str) → dict[str, Any]
def calculate_pediatric_dose(medication_name: str, patient_weight_kg: float, patient_age_years: int) → dict[str, Any]
def generate_referral_letter(patient_name: str, finding: str, severity: str) → dict[str, Any]
```

### src/voicemed/output/schemas.py
```
class SeverityLevel(str, Enum)
@dataclass TriageResult(severity, primary_concern, recommended_actions, red_flags, local_advice, referral_letter?, confidence, generated_at)
```
